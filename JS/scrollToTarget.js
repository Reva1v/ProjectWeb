function scrollToForProjects() {
    let targetElement = document.querySelector('.for_projects');
    targetElement.scrollIntoView({behavior: 'smooth'});
}
function scrollToForInvestors() {
    let targetElement = document.querySelector('.for_investors');
    targetElement.scrollIntoView({behavior: 'smooth'});
}